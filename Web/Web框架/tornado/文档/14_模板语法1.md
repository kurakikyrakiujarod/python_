### 引入 ###
```
#coding=utf-8

from tornado.template import Template

str = 'hello:{{name}}'
t = Template(str)
content = t.generate(name='zhangsan')
print content

```

### 升级1 ###

1. html页面
```

<body>
    hello--------{{name}}
</body>

```
2. py文件中读取
```
class IndexHandler(tornado.web.RequestHandler):
    def get(self, *args, **kwargs):
        with open('templates/index.html') as fr:
            content = fr.read()

        t = Template(content)
        str = t.generate(name='lisi')
        print str
        self.write(str)
```

### 升级2 ###
1. py文件中读取
```
class IndexHandler(tornado.web.RequestHandler):
    def get(self, *args, **kwargs):
        from tornado.template import Loader
        import os
        BASEDIR = os.path.join(os.getcwd(),'templates')
        #指定模板根目录
        loader = Loader(BASEDIR)
        #加载模板
        t = loader.load('index.html')
        content = t.generate(name='wangwu')
        self.write(content)
```

### 升级3 ###
```
class IndexHandler(tornado.web.RequestHandler):
    def get(self, *args, **kwargs):
        self.render('templates/index.html',name='zhaoliu')






```

### 升级4 ###

```
import os
app = tornado.web.Application([
    (r'/',IndexHandler)
   



],template_path=os.path.join(os.getcwd(),'templates'))

```



















