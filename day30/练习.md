# 7

```
d={'k1':'v1','k2':[1,2,3],('k','3'):{1,2,3}}
请用程序实现：
1）输出上述字典中value为列表的key。
2）如果字典中的key是一个元组,请输出对应的value值。
3）d[('k','3')]对应的value是一个什么数据类型。
```

```python
d = {'k1': 'v1', 'k2': [1, 2, 3], ('k', '3'): {1, 2, 3}}
func = lambda x: type(d[x]) == list
print(list(filter(func, d)))
# print(list(filter(lambda x: type(d[x]) == list, d)))

def f(d):
    for k in d:
        if type(k) == tuple:
            return d[k]

print(f(d))
print(type(d[('k', '3')]))
# ['k2']
# {1, 2, 3}
# <class 'set'>
```

# 8

如果不使用 @ wrapper装饰器，请在a()之前加入一句代码，达到相同的效果。

```
def wrapper(func):
    def inner(*arg, **kwargs):
        func(*arg, **kwargs)

    return inner


@wrapper
def a(arg):
    print(arg)


a(1)
```

```python
def wrapper(func):
    def inner(*arg, **kwargs):
        func(*arg, **kwargs)

    return inner


def a(arg):
    print(arg)


a = wrapper(a)
a(1)
```



# 9

请处理文件7th_questions，输出所有以'T'开头的行。

```python
with open("7th_questions", encoding='utf-8') as f:
    for line in f:
        line = line.strip()
        if line.startswith('T'):
            print(line)
```

